name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

permissions:
  security-events: write
  contents: read

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.24"

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: go

      - name: Build all modules
        run: |
          go build ./...
          cd twilio && go build ./... && cd ..
          cd sns && go build ./... && cd ..
          cd vonage && go build ./... && cd ..
          cd msg91 && go build ./... && cd ..

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
