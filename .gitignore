# Coverage files
coverage*.out

# Compiled test binaries
*.test

# Go build cache
*.exe
*.exe~
*.dll
*.so
*.dylib

# website
gosms-website

# go.work is deliberately committed: every published module in this repo lives
# here, and the workspace is what makes the providers compile against this tree
# instead of the published root module. Consumers never see it.
#
# go.work.sum is not. Go writes it only for sums the per-module go.sum files
# cannot supply, which happens when workspace-wide MVS lifts a dependency above
# what some module requires on its own. Committing it papers over that skew;
# leaving it out means the build says so.
go.work.sum

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
